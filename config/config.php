<?php
    // error reporting true or false
    define('DEBUG', true);

    // if there is not define the controller in the URL;
    define('DEFAULT_CONTROLLER', 'Home'); 

    // if there is no layout then the laout will be default layout
    define('DEFAULT_LAYOUT', 'default');

    // default site title
    define('DEFAULT_SITETITLE', 'php application');

    //project root
    define('PROOT', '/mvc-in-php/');